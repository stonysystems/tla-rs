# Source-First Model-Check Artifacts

This directory stores JSON artifacts generated by:

```bash
./scripts/run_model_check_matrix.sh
```

The script rewrites the matrix artifacts and `MANIFEST.txt` on each run.

# Deliberate trigger exceptions

Phase 54 replaced automatically chosen quantifier triggers with explicit ones across the hand-written tree. Its acceptance criterion allows either zero remaining notes or **this list**: every note that survives, with the reason it was not annotated.

Generated by `scripts/trigger_exceptions.py` from a measured inventory, so it cannot drift from reality unnoticed -- `--check` fails if the committed list disagrees with a fresh run.

| field | value |
|---|---|
| verus | `0.2026.08.02.b677dd5` |
| notes remaining | **103** |
| baseline | 534 (see `baseline.md`) |

| reason | notes |
|---|---:|
| generated-emitted (transpiler output) | 50 |
| generated-preserved (hand-written body inside a generated file) | 53 |

## generated-emitted — 50 note(s)

Cannot be hand-edited (`CLAUDE.md`). The codegen fix for the dominant shape landed in 54.7.a, but delivering it needs a regeneration that is blocked: the RSL modules with `skip_functions` cannot be replaced wholesale, and merging them hits signature mismatches between the preserved hand-written bodies and the current emitted API (42.8.c.2.iv). Unlike the preserved notes, **these do clear once that merge lands**.

| file | notes |
|---|---:|
| `src/generated/RSL/replica_gen.rs` | 37 |
| `src/generated/RSL/proposer_gen.rs` | 8 |
| `src/generated/RSL/learner_gen.rs` | 3 |
| `src/generated/RSL/executor_gen.rs` | 2 |

## generated-preserved — 53 note(s)

These sit in `skip_functions` bodies that regeneration copies through verbatim, so they are **not transpiler output** and regenerating will never change them -- 54.7.b cannot clear these however 42.8.c lands. They are still under `src/generated/`, so `CLAUDE.md` forbids editing them in place; the real fix is either to teach the transpiler to generate the function (see `docs/rsl-skip-functions.md` for which are capability gaps versus a deliberate trust boundary) or to move the hand-written body out to a `*_manual.rs`, as acceptor and executor already do. That is the open 54.7.c decision.

| file | notes |
|---|---:|
| `src/generated/RSL/election_gen.rs` | 17 |
| `src/generated/RSL/proposer_gen.rs` | 17 |
| `src/generated/RSL/executor_gen.rs` | 8 |
| `src/generated/RSL/replica_gen.rs` | 7 |
| `src/generated/RSL/learner_gen.rs` | 4 |


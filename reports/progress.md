# tla-rs Progress Report

Date: 2026-03-06

## 1. RSL Code Generation and Proof

The RSL (Replicated State Library) protocol now has **8 fully transpiler-generated modules** with verified exec functions:

| Module | Lines | Exec Functions | Assumes |
|--------|-------|----------------|---------|
| replica_gen.rs | 1,105 | 28 | 8 (IO trust boundary) |
| election_gen.rs | 752 | 12 | 7 |
| proposer_gen.rs | 706 | 12 | 22 |
| executor_gen.rs | 582 | 7 | 1 |
| learner_gen.rs | 399 | 4 | 5 |
| acceptor_gen.rs | 380 | 5 | 0 |
| types_gen.rs | 130 | — | 0 |
| broadcast_gen.rs | 95 | 1 | 0 |
| **Total** | **4,149** | **69** | **43** |

**Key achievements:**
- All 8 RSL modules are **standalone** (no delegates to hand-written implementation code)
- `election_gen.rs` was the last to go standalone (Phase 19.5), resolving 27 verification errors
- The 8 IO trust boundary assumes in `replica_gen.rs` (`sent_packets =~= ExtractSentPacketsFromIos(ios)`) are **irreducible** — they represent the fundamental trust that the runtime faithfully records network sends
- The remaining 35 assumes are targeted proof guidance for View-mapping gaps and `valid()` predicates

**Before vs After:** Previously, generated modules relied on `delegate` calls to hand-written `src/implementation/RSL/` code for complex functions. Now 100% of protocol logic exec functions are generated by the transpiler, with only clone helpers as `external_body`.

## 2. Glue Code: Connecting Verified Code to Networking

For each of the **10 protocols** (RSL + 9 non-RSL), Claude generated the glue code layer that connects transpiler-generated verified code to the networking runtime. This includes:

- **host.rs** — protocol host logic (message dispatch, timer management, I/O)
- **message.rs** — wire-format serialization/deserialization
- **services/** — service entry points and state machine wrappers

| Protocol | host.rs | message.rs | services/ | Total |
|----------|---------|------------|-----------|-------|
| RSL | 123 | 414 | 81 | 618 |
| Raft | 204 | 254 | 11 | 469 |
| TwoPhase | 383 | 74 | 12 | 469 |
| PrimaryBackup | 433 | 75 | 11 | 519 |
| ChainReplication | 536 | 92 | 11 | 639 |
| PBFT | 501 | 144 | 11 | 656 |
| LeaderElection | 467 | 71 | 11 | 549 |
| VerticalPaxos | 614 | 171 | 11 | 796 |
| EPaxos | 683 | 160 | 11 | 854 |
| Paxos | 456 | 130 | 11 | 597 |
| **Total** | **4,400** | **1,585** | **180** | **6,165** |

Additionally, `src/common/framework/` provides **510 LOC** of shared infrastructure (protocol traits, generic host, networking).

All 10 protocols are runnable — the integration test script (`scripts/integration_test_cluster.sh`) launches 3-node clusters and verifies startup, stability, and (for RSL/Raft) end-to-end client operations.

## 3. Raft Runnable Benchmark

The Raft protocol has a full benchmark harness with a dedicated client (`IronRaftClient.dll`). Benchmark results on localhost, 3-node cluster:

| Threads | Throughput (ops/sec) | Avg Latency (ms) |
|---------|---------------------|-------------------|
| 1 | 1,971 | 0.55 |
| 4 | 866 | 0.20 |

The benchmark demonstrates:
- Leader election works (randomized timeout [1s, 2s])
- Log replication works (AppendEntries/AppendResponse cycle)
- Client request/response works end-to-end
- Heartbeat rate-limiting (100ms interval) maintains cluster stability

Note: 4-thread aggregate throughput is lower than 1-thread because client threads contend on sending to the same leader, and each request requires a full replication round-trip. Per-thread latency is lower due to concurrent pipeline filling.

## 4. Raft Spec Enhancement (Phase 27)

**Problem identified:** After Phase 26 (Raft runnable), we reviewed the Raft code and found that the original spec only modeled atomic, fine-grained actions (11 independent state transitions). This created a **verification gap** — the host (`host.rs`) contained ~634 LOC of unverified protocol logic:

- Step-down-on-higher-term logic in every message handler
- Guard condition checks (term comparison, role checks, log-up-to-date)
- Composite action sequencing (e.g., receive vote → check quorum → become leader)
- Message construction (host manually built messages, ignoring verified `sent_packets`)

**Solution (Phase 27):** Enriched the Raft spec with **composite actions** that model complete message-handling flows:

| New Composite Spec Function | Composes |
|-----------------------------|----------|
| `LHandleRequestVoteMsg` | step_down_if_needed → guards → LGrantVote |
| `LHandleAppendEntriesMsg` | step_down_if_needed → term check → LFollowerAppendEntries |
| `LHandleVoteResponseMsg` | step_down_if_needed → role check → LReceiveVoteGranted → quorum → LBecomeLeader |
| `LHandleAppendResponseMsg` | step_down_if_needed → role check → LHandleAppendResponse / LHandleAppendReject |
| `LTryAdvanceCommitIndex` | guard check → LAdvanceCommitIndex |
| `LHandleMessage` | message dispatch to above handlers |

**Results:**
- host.rs: **978 → 204 lines** (79% reduction)
- Unverified protocol logic: **~634 → ~36 LOC** (find_commit_index + leader_heartbeat parameter computation)
- 8 new verified composite exec functions in `raft_gen.rs` (via `manual_code` injection from `raft_manual.rs`)
- Host now uses verified `sent_packets` instead of manual message construction
- New `LNext` has 5 composite branches instead of 11 atomic ones
- Refinement proof: composite `LNext` refines atomic `LNext`

See [docs/phase27-motivation.md](docs/phase27-motivation.md) for detailed pre-Phase-27 analysis.

## 5. Transpiler Enhancement (Phase 29 — Ongoing)

**Problem identified:** The 8 composite exec functions in Phase 27 had to be hand-written (`raft_manual.rs`, 369 LOC) because the transpiler cannot translate them. Root cause analysis revealed a single missing capability:

**The transpiler cannot translate value-returning spec helper functions.**

Specifically, the pattern `spec fn step_down_if_needed(s: LState, term: int) -> LState` returning an intermediate state, followed by using that intermediate state as input to another action. This is different from RSL's pattern where actions delegate to sub-components (`s.acceptor → s_.acceptor`).

**Phase 29 Plan** (in TODO.md):
1. Teach transpiler to recognize `spec fn f(s: LState, ...) -> LState` helper functions
2. Generate corresponding `exec fn Cf(s: &CState, ...) -> CState` with view-mapping proof
3. Handle intermediate state pattern: `let s_mid = f(s, ...); Action(s_mid, s_, ...)`
4. Support guard cascades on intermediate state
5. Generate composite exec functions end-to-end

**Expected impact:** If successful, all 369 LOC of `raft_manual.rs` can be auto-generated, and the pattern applies to any protocol that uses cross-cutting concerns (step-down, view-change, etc.).

## 6. Raft Safety Refinement Proof (Phase 32 + 34)

**Top-level theorem** (`lemma_refinement_correct`): every valid Raft distributed behavior refines to a sequential append-only committed log.

**Proof architecture** (6 files, ~12,000 LOC in `src/protocol/Raft/refinement_proof/`):

| File | LOC | Role |
|------|-----|------|
| `invariants.rs` | ~9,900 | Core: 30+ invariant definitions + 37+ inductive proof functions |
| `state_machine.rs` | 641 | Distributed state, RSL-style network model, ghost state |
| `message_invariants.rs` | 614 | 19 network packet invariant definitions |
| `committed.rs` | 310 | Committed log extraction + monotonicity (fully proved) |
| `refinement.rs` | 154 | Top-level refinement theorem |
| `induction.rs` | 69 | Behavior-level induction scaffolding |

**Key achievements:**
- RSL-style network model with `sentPackets`, `LRaftPacket`, receive guards, ghost `vote_log_len` state (Phase 34.1)
- 30+ invariants proved inductively: 19 message invariants, 4 ghost state, 4 SMS infrastructure (ARLA, MILA, MIB, AELCB), 3 log structure, plus structural invariants
- ElectionSafety, VotersVotedForCandidate, LogMatching fully proved (Phase 34.4-34.6)
- LeaderCompleteness partially proved: equal-term cases done, ETHVQ vote dest uniqueness resolved 3 of 10 `assume(false)` (Phase 34.7-34.9)
- StateMachineSafety spec fixed with quorum replication guard (Phase 34.8)
- Committed log prefix preservation fully proved; `committed.rs` is assume-free (Phase 34.15)
- 24 inductive lemma requires narrowed from `RaftSafetyInvariant` to minimal sub-invariants (Phase 34.15)

**Remaining assumes (12 in invariants.rs):**
- 7 LC `assume(false)`: all blocked on the `d_rli ≤ k` wall — requires leader-term strong induction (Ongaro PhD §3.6.1)
- 4 sound Z3 workarounds: ETHVQ witness extraction via `choose` crashes Z3 (permanent until Z3/Verus improves Skolemization)
- 1 SMS assume: blocked on LeaderCompleteness

See `reports/raft_refinement_proof.md` for full invariant list, assume analysis, and proof techniques.

## Summary Statistics

| Metric | Value |
|--------|-------|
| Verified functions | 669 |
| Verification errors | 0 |
| Generated code (all protocols) | 9,479 LOC |
| Raft refinement proof | ~12,000 LOC |
| RSL refinement proof assumes | 0 |
| Raft refinement proof assumes | 12 (7 LC + 4 Z3 workaround + 1 SMS) |
| Glue code (Claude-generated) | 6,165 LOC |
| Shared framework | 510 LOC |
| Protocols runnable | 10/10 |
| Raft benchmark (1-thread) | 1,971 ops/sec, 0.55ms latency |

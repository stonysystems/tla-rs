// Auto-generated by verus-transpiler
// DO NOT EDIT MANUALLY

use crate::common::collections::hashsets::clone_hashset_u64;
use crate::generated::VerticalPaxos::types_gen::*;
use crate::protocol::VerticalPaxos::types::*;
use crate::protocol::VerticalPaxos::vpaxos::*;
use std::collections::HashSet;
use vstd::prelude::*;
use vstd::set::*;
use vstd::set_lib::*;

verus! {

/// Helper proof: mapping an injective function over an empty set yields an empty set.
proof fn lemma_empty_set_map()
ensures
    Set::<u64>::empty().map(|x: u64| x as int) =~= Set::<int>::empty(),
{
    let f = |x: u64| x as int;
    let s = Set::<u64>::empty().map(f);
    assert forall|y: int| !(#[trigger] s.contains(y)) by {
    }
}

/// Helper proof: mapping over an empty Seq yields an empty Seq.
proof fn lemma_empty_seq_map()
ensures
    Seq::<u64>::empty().map(|i: int, v: u64| v as int) =~= Seq::<int>::empty(),
{
}

/// Helper proof: push commutes with Seq::map for index-ignoring functions.
proof fn lemma_seq_push_map_commute(s: Seq<u64>, x: u64)
ensures
    s.push(x).map(|i: int, v: u64| v as int) =~= s.map(|i: int, v: u64| v as int).push(x as int),
{
}


pub exec fn CInit(c: &CConstants) -> (result: CState)
requires
    c.valid(),
    (c.quorum_size >= 1),
    (c.num_nodes >= c.quorum_size),
ensures
    result.valid(),
    LInit(result@, c@),
{
    let result = CState {
        config_num: 0u64,
        max_bal: 0u64,
        max_v_bal: 0u64,
        max_val: 0u64,
        has_voted: false,
        is_active: true,
        promises_rcvd: HashSet::new(),
        accepts_rcvd: HashSet::new(),
        committed: false,
        committed_val: 0u64,
        witness_val: 0u64,
        has_witness: false,
    };
    proof {
        lemma_empty_set_map();
    }
    result

}

impl CState {
    pub exec fn CPrepare(&mut self, c: &CConstants, b: &u64) -> (result: Vec<CVPMessage>)
    requires
        old(self).valid(),
        c.valid(),
        old(self).is_active == true,
        (*b > old(self).max_bal),
    ensures
        self.valid(),
        LPrepare(old(self)@, self@, c@, *b as int, result@.map(|i, p: CVPMessage| p@)),
    {
        let ghost old_self = *old(self);
        self.max_bal = (*b);
        self.max_v_bal = self.max_v_bal.clone();
        self.max_val = self.max_val.clone();
        self.has_voted = self.has_voted.clone();
        self.config_num = self.config_num.clone();
        self.is_active = self.is_active.clone();
        self.promises_rcvd = clone_hashset_u64(&self.promises_rcvd);
        self.accepts_rcvd = clone_hashset_u64(&self.accepts_rcvd);
        self.committed = self.committed.clone();
        self.committed_val = self.committed_val.clone();
        self.witness_val = self.witness_val.clone();
        self.has_witness = self.has_witness.clone();
        let result = vec![CVPMessage::Prepare {
    bal: (*b),
}];
        proof {
            assert(result@.map(|i: int, p: CVPMessage| p@) =~= Seq::empty().push(result@[0]@));
        }
        result

    }
}

impl CState {
    pub exec fn CSendPromise(&mut self, c: &CConstants, prepare_bal: &u64) -> (result: Vec<CVPMessage>)
    requires
        old(self).valid(),
        c.valid(),
        old(self).is_active == true,
        (*prepare_bal > old(self).max_bal),
    ensures
        self.valid(),
        LSendPromise(old(self)@, self@, c@, *prepare_bal as int, result@.map(|i, p: CVPMessage| p@)),
    {
        let ghost old_self = *old(self);
        self.max_bal = (*prepare_bal);
        self.max_v_bal = self.max_v_bal.clone();
        self.max_val = self.max_val.clone();
        self.has_voted = self.has_voted.clone();
        self.config_num = self.config_num.clone();
        self.is_active = self.is_active.clone();
        self.promises_rcvd = clone_hashset_u64(&self.promises_rcvd);
        self.accepts_rcvd = clone_hashset_u64(&self.accepts_rcvd);
        self.committed = self.committed.clone();
        self.committed_val = self.committed_val.clone();
        self.witness_val = self.witness_val.clone();
        self.has_witness = self.has_witness.clone();
        let result = vec![CVPMessage::Promise {
    bal: (*prepare_bal),
    v_bal: self.max_v_bal.clone(),
    val: self.max_val.clone(),
}];
        proof {
            assert(result@.map(|i: int, p: CVPMessage| p@) =~= Seq::empty().push(result@[0]@));
        }
        result

    }
}

impl CState {
    pub exec fn CReceivePromise(&mut self, c: &CConstants, sender: &u64, promise_bal: &u64, promise_v_bal: &u64, promise_val: &u64) -> (result: Vec<CVPMessage>)
    requires
        old(self).valid(),
        c.valid(),
        old(self).is_active == true,
        *promise_bal == old(self).max_bal,
        !old(self)@.promises_rcvd.contains(*sender as int),
    ensures
        self.valid(),
        LReceivePromise(old(self)@, self@, c@, *sender as int, *promise_bal as int, *promise_v_bal as int, *promise_val as int, result@.map(|i, p: CVPMessage| p@)),
    {
        let ghost old_self = *old(self);
        let mut __promises_rcvd = clone_hashset_u64(&self.promises_rcvd);
        __promises_rcvd.insert(sender.clone());
        let result = {
            proof {
                lemma_empty_seq_map();
            }
            { self.promises_rcvd = __promises_rcvd; self.max_v_bal = if ((*promise_v_bal) > self.max_v_bal) {
                (*promise_v_bal)
            } else {
                self.max_v_bal.clone()
            }; self.max_val = if ((*promise_v_bal) > self.max_v_bal) {
                (*promise_val)
            } else {
                self.max_val.clone()
            }; self.max_bal = self.max_bal.clone(); self.has_voted = self.has_voted.clone(); self.config_num = self.config_num.clone(); self.is_active = self.is_active.clone(); self.accepts_rcvd = clone_hashset_u64(&self.accepts_rcvd); self.committed = self.committed.clone(); self.committed_val = self.committed_val.clone(); self.witness_val = self.witness_val.clone(); self.has_witness = self.has_witness.clone(); vec![] }
        };
        proof {
            broadcast use Set::lemma_set_map_insert_commute;
            lemma_empty_seq_map();
            assert(result@.map(|i: int, p: CVPMessage| p@) =~= Seq::empty());
        }
        result

    }
}

impl CState {
    pub exec fn CAccept(&mut self, c: &CConstants, b: &u64, v: &u64) -> (result: Vec<CVPMessage>)
    requires
        old(self).valid(),
        c.valid(),
        old(self).is_active == true,
        *b == old(self).max_bal,
        (*b > old(self).max_v_bal),
    ensures
        self.valid(),
        LAccept(old(self)@, self@, c@, *b as int, *v as int, result@.map(|i, p: CVPMessage| p@)),
    {
        let ghost old_self = *old(self);
        self.max_v_bal = (*b);
        self.max_val = (*v);
        self.has_voted = true;
        self.max_bal = self.max_bal.clone();
        self.config_num = self.config_num.clone();
        self.is_active = self.is_active.clone();
        self.promises_rcvd = clone_hashset_u64(&self.promises_rcvd);
        self.accepts_rcvd = clone_hashset_u64(&self.accepts_rcvd);
        self.committed = self.committed.clone();
        self.committed_val = self.committed_val.clone();
        self.witness_val = self.witness_val.clone();
        self.has_witness = self.has_witness.clone();
        let result = vec![CVPMessage::Accept {
    bal: (*b),
    val: (*v),
}];
        proof {
            assert(result@.map(|i: int, p: CVPMessage| p@) =~= Seq::empty().push(result@[0]@));
        }
        result

    }
}

impl CState {
    pub exec fn CReceiveAccepted(&mut self, c: &CConstants, sender: &u64, accept_bal: &u64) -> (result: Vec<CVPMessage>)
    requires
        old(self).valid(),
        c.valid(),
        old(self).is_active == true,
        *accept_bal == old(self).max_bal,
        !old(self)@.accepts_rcvd.contains(*sender as int),
    ensures
        self.valid(),
        LReceiveAccepted(old(self)@, self@, c@, *sender as int, *accept_bal as int, result@.map(|i, p: CVPMessage| p@)),
    {
        let ghost old_self = *old(self);
        let mut __accepts_rcvd = clone_hashset_u64(&self.accepts_rcvd);
        __accepts_rcvd.insert(sender.clone());
        let result = {
            proof {
                lemma_empty_seq_map();
            }
            { self.accepts_rcvd = __accepts_rcvd; self.max_bal = self.max_bal.clone(); self.max_v_bal = self.max_v_bal.clone(); self.max_val = self.max_val.clone(); self.has_voted = self.has_voted.clone(); self.config_num = self.config_num.clone(); self.is_active = self.is_active.clone(); self.promises_rcvd = clone_hashset_u64(&self.promises_rcvd); self.committed = self.committed.clone(); self.committed_val = self.committed_val.clone(); self.witness_val = self.witness_val.clone(); self.has_witness = self.has_witness.clone(); vec![] }
        };
        proof {
            broadcast use Set::lemma_set_map_insert_commute;
            lemma_empty_seq_map();
            assert(result@.map(|i: int, p: CVPMessage| p@) =~= Seq::empty());
        }
        result

    }
}

impl CState {
    pub exec fn CCommit(&mut self, c: &CConstants) -> (result: Vec<CVPMessage>)
    requires
        old(self).valid(),
        c.valid(),
        old(self).is_active == true,
        old(self).committed == false,
        (old(self)@.accepts_rcvd.len() >= c.quorum_size),
    ensures
        self.valid(),
        LCommit(old(self)@, self@, c@, result@.map(|i, p: CVPMessage| p@)),
    {
        let ghost old_self = *old(self);
        proof {
            lemma_empty_seq_map();
        }
        let result = {
            self.committed = true;
            self.committed_val = self.max_val.clone();
            self.max_bal = self.max_bal.clone();
            self.max_v_bal = self.max_v_bal.clone();
            self.max_val = self.max_val.clone();
            self.has_voted = self.has_voted.clone();
            self.config_num = self.config_num.clone();
            self.is_active = self.is_active.clone();
            self.promises_rcvd = clone_hashset_u64(&self.promises_rcvd);
            self.accepts_rcvd = clone_hashset_u64(&self.accepts_rcvd);
            self.witness_val = self.witness_val.clone();
            self.has_witness = self.has_witness.clone();
            vec![]
        };
        proof {
            lemma_empty_seq_map();
            assert(result@.map(|i: int, p: CVPMessage| p@) =~= Seq::empty());
        }
        result

    }
}

impl CState {
    pub exec fn CReconfigure(&mut self, c: &CConstants) -> (result: Vec<CVPMessage>)
    requires
        old(self).valid(),
        c.valid(),
        old(self).is_active == true,
        old(self).config_num < u64::MAX,
    ensures
        self.valid(),
        LReconfigure(old(self)@, self@, c@, result@.map(|i, p: CVPMessage| p@)),
    {
        let ghost old_self = *old(self);
        proof {
            lemma_empty_seq_map();
        }
        let result = {
            self.config_num = (self.config_num + 1);
            self.max_bal = 0u64;
            self.max_v_bal = 0u64;
            self.max_val = self.max_val.clone();
            self.has_voted = false;
            self.is_active = true;
            self.promises_rcvd = HashSet::new();
            self.accepts_rcvd = HashSet::new();
            self.committed = self.committed.clone();
            self.committed_val = self.committed_val.clone();
            self.witness_val = self.witness_val.clone();
            self.has_witness = self.has_witness.clone();
            vec![]
        };
        proof {
            lemma_empty_set_map();
            lemma_empty_seq_map();
            assert(result@.map(|i: int, p: CVPMessage| p@) =~= Seq::empty());
        }
        result

    }
}

impl CState {
    pub exec fn CWitnessSync(&mut self, c: &CConstants, witness_val: &u64) -> (result: Vec<CVPMessage>)
    requires
        old(self).valid(),
        c.valid(),
        old(self).is_active == true,
    ensures
        self.valid(),
        LWitnessSync(old(self)@, self@, c@, *witness_val as int, result@.map(|i, p: CVPMessage| p@)),
    {
        let ghost old_self = *old(self);
        proof {
            lemma_empty_seq_map();
        }
        let result = {
            self.has_witness = true;
            self.witness_val = (*witness_val);
            self.max_val = if !self.has_voted {
                (*witness_val)
            } else {
                self.max_val.clone()
            };
            self.max_bal = self.max_bal.clone();
            self.max_v_bal = self.max_v_bal.clone();
            self.has_voted = self.has_voted.clone();
            self.config_num = self.config_num.clone();
            self.is_active = self.is_active.clone();
            self.promises_rcvd = clone_hashset_u64(&self.promises_rcvd);
            self.accepts_rcvd = clone_hashset_u64(&self.accepts_rcvd);
            self.committed = self.committed.clone();
            self.committed_val = self.committed_val.clone();
            vec![]
        };
        proof {
            lemma_empty_seq_map();
            assert(result@.map(|i: int, p: CVPMessage| p@) =~= Seq::empty());
        }
        result

    }
}

impl CState {
    pub exec fn CSync(&mut self, c: &CConstants, new_config: &u64, val: &u64) -> (result: Vec<CVPMessage>)
    requires
        old(self).valid(),
        c.valid(),
        old(self).is_active == false,
        (*new_config > old(self).config_num),
    ensures
        self.valid(),
        LSync(old(self)@, self@, c@, *new_config as int, *val as int, result@.map(|i, p: CVPMessage| p@)),
    {
        let ghost old_self = *old(self);
        proof {
            lemma_empty_seq_map();
        }
        let result = {
            self.config_num = (*new_config);
            self.max_bal = 0u64;
            self.max_v_bal = 0u64;
            self.max_val = (*val);
            self.has_voted = false;
            self.is_active = true;
            self.promises_rcvd = HashSet::new();
            self.accepts_rcvd = HashSet::new();
            self.committed = false;
            self.committed_val = 0u64;
            self.witness_val = 0u64;
            self.has_witness = false;
            vec![]
        };
        proof {
            lemma_empty_set_map();
            lemma_empty_seq_map();
            assert(result@.map(|i: int, p: CVPMessage| p@) =~= Seq::empty());
        }
        result

    }
}

impl CState {
    pub exec fn CDeactivate(&mut self, c: &CConstants) -> (result: Vec<CVPMessage>)
    requires
        old(self).valid(),
        c.valid(),
        old(self).is_active == true,
    ensures
        self.valid(),
        LDeactivate(old(self)@, self@, c@, result@.map(|i, p: CVPMessage| p@)),
    {
        let ghost old_self = *old(self);
        proof {
            lemma_empty_seq_map();
        }
        let result = {
            self.is_active = false;
            self.config_num = self.config_num.clone();
            self.max_bal = self.max_bal.clone();
            self.max_v_bal = self.max_v_bal.clone();
            self.max_val = self.max_val.clone();
            self.has_voted = self.has_voted.clone();
            self.promises_rcvd = clone_hashset_u64(&self.promises_rcvd);
            self.accepts_rcvd = clone_hashset_u64(&self.accepts_rcvd);
            self.committed = self.committed.clone();
            self.committed_val = self.committed_val.clone();
            self.witness_val = self.witness_val.clone();
            self.has_witness = self.has_witness.clone();
            vec![]
        };
        proof {
            lemma_empty_seq_map();
            assert(result@.map(|i: int, p: CVPMessage| p@) =~= Seq::empty());
        }
        result

    }
}

} // verus!

# Configuration for transpiling RSL election spec to implementation
# This configures the transpiler to match the existing RSL codebase conventions

[naming]
spec_prefix = "L"
exec_prefix = "C"

[remapping]
# RSL-specific type mappings
"ElectionState" = "CElectionState"
"LReplicaConstants" = "CReplicaConstants"
"Ballot" = "CBallot"
"RslPacket" = "CPacket"
"Request" = "CRequest"
"RequestBatch" = "CRequestBatch"

[output]
generate_abstraction_fns = true
generate_validity_predicates = true
# RSL uses 'valid()' instead of 'well_formed()'
validity_predicate_name = "valid"
generate_clone = true
include_debug_comments = false
# Generate explicit for loops instead of iterators for better Verus verification
generate_loops_for_verification = true
# Generate CElectionState inline instead of importing from ElectionImpl.rs
generate_inline_types = true
# Custom imports for election module
# Note: Shared infrastructure types (types_i, cconstants, cmessage, cconfiguration)
# contain marshalling-enabled types that can't easily be generated.
# Future work: move these to src/common/rsl_types/ for better organization.
# CElectionState is generated inline (module-specific state).
custom_imports = [
    "use vstd::prelude::*;",
    "use vstd::map::*;",
    "use vstd::set::*;",
    "use std::collections::HashSet;",
    "use crate::common::collections::sets::*;",
    "use crate::common::collections::hashsets::*;",
    "use crate::common::collections::vecs::*;",
    "use crate::common::native::io_s::EndPoint;",
    "use crate::implementation::RSL::types_i::*;",
    "use crate::implementation::RSL::cconstants::*;",
    "use crate::implementation::RSL::cmessage::*;",
    "use crate::implementation::RSL::cconfiguration::*;",
    "use crate::implementation::common::upper_bound_i::*;",
    "use crate::protocol::RSL::election::*;",
    "use crate::protocol::RSL::types::*;",
    "use crate::protocol::RSL::configuration::*;",
]

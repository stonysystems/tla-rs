# Configuration for generating RSL types
# This is separate from the main transpile.toml for function generation

[naming]
spec_prefix = "L"
exec_prefix = "C"
# RSL implementation uses u64 for all numeric types for marshalling compatibility
int_type = "u64"
nat_type = "u64"

[remapping]
# External types that don't follow L->C convention
"AbstractEndPoint" = "EndPoint"
"AppMessage" = "CAppMessage"
"RequestBatch" = "CRequestBatch"

[output]
generate_abstraction_fns = true
generate_validity_predicates = true
validity_predicate_name = "well_formed"
generate_clone = true
include_debug_comments = false
# Custom imports for RSL types
# Note: CAppMessage has marshalling support for network serialization.
# CRequestBatch is now defined as a type alias in types_gen.rs itself.
custom_imports = [
    "use vstd::prelude::*;",
    "use std::collections::{HashMap, HashSet};",
    "use crate::common::collections::hashsets::HashSetWellFormed;",
    "use crate::common::framework::environment_s::LIoOp;",
    "use crate::common::native::io_s::EndPoint;",
    "use crate::implementation::RSL::appinterface::CAppMessage;",
    "use crate::implementation::RSL::cmessage::CPacket;",
    "use crate::implementation::RSL::ReplicaImpl::CReplica;",
    "use crate::protocol::RSL::environment::RslIo;",
    "use crate::protocol::RSL::replica::LScheduler;",
    "use crate::protocol::RSL::types::*;",
]

module PBFT::pbft {
    LInit(-, +);
    LPrePrepare(+, -, +, +);
    LReceivePrePrepare(+, -, +);
    LReceivePrepare(+, -, +, +);
    LReceiveCommit(+, -, +, +);
    LCheckpoint(+, -, +, +);
    LViewChange(+, -, +);
    LNewRound(+, -, +);
    LEnterCommit(+, -, +);
    LExecuteReply(+, -, +);
    // LNext not transpiled (contains existential quantifiers)
}

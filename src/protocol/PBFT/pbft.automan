module PBFT::pbft {
    LInit(-, +);
    LPrePrepare(+, -, +);
    LReceivePrepare(+, -, +);
    LEnterCommit(+, -, +);
    LReceiveCommit(+, -, +);
    LExecuteReply(+, -, +);
    LViewChange(+, -, +);
    LNewRound(+, -, +);
}

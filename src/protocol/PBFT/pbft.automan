module PBFT::pbft {
    LInit(-, +);
    LPrePrepare(+, -, +, +);
    LReceivePrePrepare(+, -, +);
    LReceivePrepare(+, -, +, +);
    LReceiveCommit(+, -, +, +);
    LCheckpoint(+, -, +, +);
    LViewChange(+, -, +);
    LNewRound(+, -, +);
    // LEnterCommit not transpiled (uses Set::len() for quorum check)
    // LExecuteReply not transpiled (uses Set::len() for quorum check)
    // LNext not transpiled (contains existential quantifiers)
}

[package]
name = "dpor-checker"
version = "0.1.0"
edition = "2021"
description = "DPOR-based model checker prototype for tla-rs specifications"

[dependencies]
verus-transpiler = { path = ".." }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"

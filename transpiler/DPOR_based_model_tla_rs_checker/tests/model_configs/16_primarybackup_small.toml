# Model config — case 16 runtime-closure profile (38.15.3)
#
# Selected reproducible row (2026-04-10):
# - result: ok
# - stop_reason: FrontierExhausted
# - initial_states: 1
# - distinct_states: 211
# - command: verus-transpile model-check ... --model this_file --invariant LSafetyInactiveStateIsQuiescent --json-report

[constants.assignments]
State = 0
PBMessage = 0
Constants = 0
max_log_len = 1

[search]
max_depth = 6
max_states = 20000
timeout_ms = 45000

[properties]
invariants = []
check_deadlock = false
successor_semantics = "deadlock"

[quantifiers]
int = { min = 0, max = 1 }

[collections]
max_set_len = 1
max_seq_len = 1
max_map_len = 1

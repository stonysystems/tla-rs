[constants.assignments]
limit = 2

[quantifiers.int]
min = 0
max = 2

[search]
max_depth = 4
max_states = 100
timeout_ms = 1000

[properties]
leads_to = [{ name = "eventual_two", from = "LFrom", to = "LTo" }]
fairness = { strong = ["branch_2", "branch_3"] }

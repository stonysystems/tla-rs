------------------------------- MODULE TLAPS -------------------------------
(* Minimal stub so TLC can parse a spec that EXTENDS TLAPS. The real module *)
(* supplies proof-backend names, which TLC ignores; they only have to exist *)
(* for the parser.                                                          *)
SMT == TRUE
Z3 == TRUE
CVC3 == TRUE
Zenon == TRUE
Isa == TRUE
Auto == TRUE
Blast == TRUE
Force == TRUE
PTL == TRUE
SimpleArithmetic == TRUE
=============================================================================

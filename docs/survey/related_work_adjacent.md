# Adjacent Work: NL -> Formal Specification and Related Methods

Works that do not directly target text-to-TLA+ but address closely related problems with transferable methods.

## Categories

This document organizes adjacent work into the following categories:

### 1. Natural Language -> Formal Logic / Specification

Works that translate natural language into formal specifications in languages other than TLA+ (e.g., LTL, CTL, Alloy, Z, Event-B, Dafny, Coq, Isabelle).

*(Individual work entries to be added after search.)*

### 2. Natural Language -> State Machine / Automata / Workflow Extraction

Works that extract state machines, finite automata, or workflow models from natural-language descriptions.

*(Individual work entries to be added after search.)*

### 3. NL -> Code Generation with Formal Structure

LLM-based methods that generate formally structured outputs (verified code, typed specifications, constraint systems) from text.

*(Individual work entries to be added after search.)*

### 4. Grammar-Constrained / Syntax-Constrained Generation

Methods for constraining LLM output to conform to a formal grammar or schema during generation, applicable to ensuring syntactically valid TLA+ output.

*(Individual work entries to be added after search.)*

### 5. Retrieval-Augmented Generation for Spec/Code Tasks

RAG and tool-using agent approaches applied to specification or code generation tasks.

*(Individual work entries to be added after search.)*

### 6. Program Repair / Self-Refinement / Verifier-in-the-Loop

Methods that use verification feedback (type errors, model-checking counterexamples, proof failures) to iteratively refine generated artifacts. These may transfer to a TLA+ generation loop where TLC/SANY feedback guides correction.

*(Individual work entries to be added after search.)*

## Synthesis

*(Cross-cutting observations about which adjacent methods are most promising for text-to-TLA+ -- to be written after individual work reviews.)*
